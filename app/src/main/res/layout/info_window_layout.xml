<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="200dp"
    android:layout_height="100dp"
    android:background="@drawable/info_window_background">

    <ImageView
        android:id="@+id/iw_ic_pin"
        android:layout_width="25sp"
        android:layout_height="25sp"
        android:src="@mipmap/ic_pin_64_foreground"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dim Anagnostou 9, Kapandriti, 19014"
        android:textColor="@color/black"
        app:layout_constraintStart_toEndOf="@+id/iw_ic_pin"/>

</androidx.constraintlayout.widget.ConstraintLayout>